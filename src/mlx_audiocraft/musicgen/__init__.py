"""MusicGen MLX runtime internals.

This package contains the internal services for MLX-native MusicGen inference:
checkpoint resolution, config parsing, weight conversion, caching, and runtime
assembly. These are consumed by the public MusicGen.from_pretrained API.
"""
